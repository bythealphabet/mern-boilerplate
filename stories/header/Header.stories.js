import React from "react";
import Header from "../../client/containers/navigation/header/Header";

export default {
  title: "Header/Header",
  component: Header,
};

export const Default = (args) => <Header {...args} />;
